<section class="content hidden-print" id="realisations">
    <div class="page-header">
        <h2>Mes réalisations</h2>
    </div>
    <div class="alert alert-warning">
        <b>Attention !</b> Seuls mes projets personnels, en ligne et encore d'actualité sont présentés ici. De nombreux autres projets sont "dans les cartons", ou "en sommeils" et ne sont pas ou plus représentatifs.
        Si vous voulez en voir plus, n'hésitez pas à <a href="#contact" data-handler="scroll">me contacter</a>.
    </div>
    <hr />

    {% for realisation in resume.realisations %}
        <div class="row">
            <div class="col-xs-4">
                <a href="{{ realisation.url }}" target="_blank"><img class="img-responsive" src="http://www.apercite.fr/api/apercite/320x240/non/{{ realisation.url }}"></a>
            </div>
            <div class="col-xs-8">
                <i class="text-muted pull-right">{{ realisation.startedAt | localizeddate('none', 'none', 'fr', null, 'MMMM yyyy') }}</i>
                <h4>{{ realisation.title }}</h4>
                <p><a href="{{ realisation.url }}" target="_blank">{{ realisation.urlLabel | default(realisation.url) }}</a></p>
                {{ realisation.description | raw }}
            </div>
        </div>
        <hr>
    {% endfor %}
</section>
